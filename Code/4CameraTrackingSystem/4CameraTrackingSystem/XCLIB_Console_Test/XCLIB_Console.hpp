#ifndef _XCLIB_CONSOLE_H
#define _XCLIB_CONSOLE_H	1

#ifndef max
#define max(a,b)            (((a) > (b)) ? (a) : (b))
#endif

#ifndef min
#define min(a,b)            (((a) < (b)) ? (a) : (b))
#endif


typedef unsigned int UINT;
typedef unsigned long DWORD, *PDWORD, *LPDWORD;

#endif